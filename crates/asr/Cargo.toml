[package]
name = "echovoice-asr"
version = "0.1.0"
edition = "2021"

[dependencies]
# whisper-rs = "0.8"  # TODO: Re-enable after cmake version fix
thiserror = "1.0"

[dev-dependencies]
anyhow = "1.0"